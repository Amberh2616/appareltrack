{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export DATABASE_URL=${DATABASE_URL:-postgresql://postgres:uBBgFUYjylnaEhUIAKuLhOiwIXqtGSbJ@postgres.railway.internal:5432/railway} && echo '[SHELL] DATABASE_URL='${DATABASE_URL:+SET} && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
