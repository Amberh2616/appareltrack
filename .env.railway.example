# ============================================================
# Railway 後端環境變數（在 Railway dashboard > Variables 填入）
# ============================================================

# Django
DJANGO_SETTINGS_MODULE=config.settings.production
DJANGO_SECRET_KEY=change-me-to-a-long-random-string-50-chars

# 允許的 Host（Railway 自動分配的域名 + 自訂域名）
# 範例: yourapp.railway.app,appareltrack.com
DJANGO_ALLOWED_HOSTS=yourapp.railway.app

# Database（Railway PostgreSQL 外掛會自動注入 DATABASE_URL，不需手動填）
# DATABASE_URL=postgresql://...

# Redis（Railway Redis 外掛會自動注入 REDIS_URL，不需手動填）
# REDIS_URL=redis://...

# CORS — 填入 Vercel 前端 URL
CORS_ALLOWED_ORIGINS=https://yourapp.vercel.app

# OpenAI
OPENAI_API_KEY=sk-...

# Email（SendGrid SMTP）
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=SG.xxxxxxxxxxxxxxxx
DEFAULT_FROM_EMAIL=noreply@appareltrack.com

# Sentry（可選，錯誤監控）
# SENTRY_DSN=https://...

# Cloudflare R2（先不開，Demo 階段用本地存檔）
# USE_R2=false
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=fashion-prod
# R2_ENDPOINT_URL=https://<accountid>.r2.cloudflarestorage.com

# ============================================================
# Vercel 前端環境變數（在 Vercel dashboard > Settings > Env 填入）
# ============================================================

# 後端 API URL（Railway 部署完成後取得）
NEXT_PUBLIC_API_URL=https://yourapp.railway.app/api/v2
